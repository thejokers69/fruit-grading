// src/config.js
const config = {
    database: {
        username: 'root',
        password: 'root',
        database: 'fruit-grading',
        host: 'localhost',
        dialect: 'mysql',
    }
};

export default config;